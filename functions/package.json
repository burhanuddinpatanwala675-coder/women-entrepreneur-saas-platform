{
  "name": "hercommerce-functions",
  "description": "AI Assistant only — real architecture, intentionally NOT deployed. See functions/README.md and ARCHITECTURE.md's migration note (this build avoids Cloud Functions entirely to stay card-free).",
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "type": "commonjs",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "lint": "eslint --ext .js,.ts .",
    "test": "echo \"no tests yet\" && exit 0"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.3.1"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.15.0",
    "@typescript-eslint/parser": "^8.15.0",
    "eslint": "^8.57.1",
    "typescript": "^5.6.3"
  },
  "private": true
}
